export class Device {
  id: string;
  autoConnect: boolean;
  client: string;
  connection: string;
  isConnected: boolean;
  lastConnected: Date;
  port: string;
  timestamp: Date;
  type: string;
}
